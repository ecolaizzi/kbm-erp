<Window x:Class="KBM.Client.Views.ItemPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleziona articoli" Height="520" Width="720"
        WindowStartupLocation="CenterOwner" Background="{StaticResource Brush.Surface}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Seleziona uno o piu articoli" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock DockPanel.Dock="Left" Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                       Foreground="{StaticResource Brush.TextSecondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="SearchBox" Height="26" VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged" ToolTip="Filtra per codice o descrizione"/>
        </DockPanel>

        <DataGrid Grid.Row="2" x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" HeadersVisibility="Column" SelectionMode="Extended"
                  Style="{StaticResource KbmDataGrid}" MouseDoubleClick="Grid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Codice" Binding="{Binding Code}" Width="120"/>
                <DataGridTextColumn Header="Descrizione" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" Width="140"/>
                <DataGridTextColumn Header="UM" Binding="{Binding UnitOfMeasure}" Width="60"/>
                <DataGridTextColumn Header="Prezzo" Binding="{Binding BasePrice, StringFormat=N2}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock x:Name="HintText" DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="11"
                       Foreground="{StaticResource Brush.TextSecondary}" Text="Doppio click o Aggiungi selezionati"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Aggiungi selezionati" Style="{StaticResource KbmPrimary}" Height="28" Padding="16,0" Margin="0,0,8,0" Click="Ok_Click"/>
                <Button Content="Annulla" Height="28" Padding="16,0" Click="Cancel_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
